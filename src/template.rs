use anyhow::Result;
use std::path::Path;

pub fn task_01(data_path: &Path) -> Result<String> {
    Ok(format!("Answer: {}", 42))
}

pub fn task_02(data_path: &Path) -> Result<String> {
    Ok(format!("Answer: {}", 42))
}
